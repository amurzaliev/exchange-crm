{% extends "profile.layout.html.twig" %}
{% block content %}

    <section class="content-header">
        <h1>
            Группы привилегий
            <small>Добавления</small>
        </h1>
        <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Главная</a></li>
        </ol>
    </section>


    <!-- Main content -->
    <section class="content">
        <div class="row">
            <!--/.col (left) -->
            <!-- right column -->
            <div class="col-md-12">
                <!-- /.box -->
                <!-- general form elements disabled -->
                <div class="box box-warning">
                    <div class="box-header with-border">
                        <h3 class="box-title">Добавления новой Группы привилегий</h3>
                    </div>
                    <!-- /.box-header -->
                    <div class="box-body">
                        {{ form_start(form) }}

                        {{ form_label(form.title, 'Название') }}
                        {{ form_errors(form.title) }}
                        {{ form_widget(form.title) }}
                        {{ form_help(form.title) }}

                        {{ form_label(form.alias, 'Альяс') }}
                        {{ form_errors(form.alias) }}
                        {{ form_widget(form.alias) }}
                        {{ form_help(form.alias) }}

                        <h3>Список пиривилегий</h3>

                        {{ form_errors(form.create_personal) }}
                        {{ form_widget(form.create_personal, {'attr' : {
                            'class' : 'minimal'
                        }}) }}
                        {{ form_help(form.create_personal) }}

                        {{ form_errors(form.edit_personal) }}
                        {{ form_widget(form.edit_personal, {'attr' : {
                            'class' : 'minimal'
                        }}) }}
                        {{ form_help(form.edit_personal) }}

                        {{ form_errors(form.view_personal) }}
                        {{ form_widget(form.view_personal, {'attr' : {
                            'class' : 'minimal'
                        }}) }}
                        {{ form_help(form.view_personal) }}

                        <hr>

                        {{ form_widget(form.save, {'label' : 'Добавить', 'attr' : {
                            'class' : 'btn btn-primary btn-lg'
                        }}) }}
                        {{ form_end(form) }}
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>
            <!--/.col (right) -->
        </div>
        <!-- /.row -->
    </section>
{% endblock %}